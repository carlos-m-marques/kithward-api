ActiveAdmin.register Unit do

  # See permitted parameters documentation:
  # https://github.com/activeadmin/activeadmin/blob/master/docs/2-resource-customization.md#setting-up-strong-parameters
  #
  # Uncomment all parameters which should be permitted for assignment
  #
  # permit_params :name, :is_available, :date_available, :rent_market, :listing_id, :unit_number, :building_id, :unit_type_id, :deleted_at, :flagged_at, :flagged_for
  #
  # or
  #
  # permit_params do
  #   permitted = [:name, :is_available, :date_available, :rent_market, :listing_id, :unit_number, :building_id, :unit_type_id, :deleted_at, :flagged_at, :flagged_for]
  #   permitted << :other if params[:action] == 'create' && current_user.admin?
  #   permitted
  # end
  
end
